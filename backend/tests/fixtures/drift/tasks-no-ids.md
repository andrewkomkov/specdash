# Tasks: A hand-written list with no task ids at all

Some `tasks.md` files are written by a person rather than generated. They carry no
`T###` anywhere, and refusing to count them would show the feature as having no work.

## Phase 1: Setup

- [x] Create the repository
- [x] Add the linter

## Phase 2: Build it

- [x] Write the parser
- [ ] Write the tests
- [ ] Ship it
