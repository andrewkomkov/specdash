## What this changes

<!-- One paragraph. What was wrong or missing, and what is different now. -->

## Which feature it belongs to

<!-- The `specs/NNN-slug/` this works against, or the new one it adds. Code before a spec is
     the one thing this project holds itself to avoiding. -->

## Checks

- [ ] `cd backend && python -m pytest` — passes, coverage still 100%
- [ ] `cd frontend && npm run build` — typechecks and builds
- [ ] `cd e2e && npx playwright test` — passes
- [ ] Task list updated, and every box I ticked is a claim about code I checked
- [ ] No new write path into a scanned project, and nothing new loaded from the internet
