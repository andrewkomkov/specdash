{
  "name": "specdash-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "End-to-end suite driving the real application against a checked-in fixture workspace",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
